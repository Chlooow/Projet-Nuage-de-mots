/* Variables pour les couleurs */
:root {
    --blue-light: #90CAF9; /* Bleu très clair, ciel */
    --blue-mid: #64B5F6; /* Bleu moyen */
    --blue-dark: #2196F3; /* Bleu foncé pour le texte/boutons */
    --white: #FFFFFF;
    --border-radius-xl: 30px;
}

* {
    box-sizing: border-box;
    font-family: 'Poppins', sans-serif;
}

/* Corps de la page (l'arrière-plan ciel dégradé) */
body {
    margin: 0;
    min-height: 100vh;
    background: linear-gradient(180deg, var(--blue-light) 0%, var(--blue-mid) 100%);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: stretch;
    margin-bottom: 40px;
}

.app-container {
    width: 100%;
    max-width: 1400px;
    /* height: 100vh; */
    flex-grow: 1;
    padding: 20px;
    display: flex;
    flex-direction: column;
}

/* --- EN-TÊTE (Header) --- */
header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 10px 0 20px;
}

/* Roue dentée */
.icone-parametres i { /* Ciblage de la balise <i> de Font Awesome */
    font-size: 35px;
    color: var(--white);
    cursor: pointer;
    opacity: 0.8;
}

/* Logo PNG */
.logo-image {
    height: 100px; 
    width: auto;
    margin-top: -30px; 
}

/* --- CONTENEUR PRINCIPAL (Main - Dual Layout) --- */
main {
    flex-grow: 1; 
    display: flex;
    gap: 30px; 
    align-items: flex-start; 
}

.conteneur-saisie, .conteneur-resultat {
    display: flex;
    flex-direction: column;
    height: 100%;
}

.conteneur-saisie {
    flex: 4; 
    align-items: center;
}

.conteneur-resultat {
    flex: 6; 
}

/* Style de base des deux grandes boîtes blanches */
.box-style {
    background-color: var(--white);
    padding: 20px;
    border-radius: var(--border-radius-xl);
    border: 3px solid var(--white); 
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    width: 95%;
    height: 300px; 
    position: relative;
    overflow: hidden;
}

.boite-saisie-texte {
    border: 3px solid var(--blue-mid); /* Bordure bleue pour l'input */
}

/* --- TEXTAREA (#saisie-texte) --- */
#saisie-texte {
    width: 100%;
    height: 100%;
    padding: 10px;
    border: none;
    resize: none;
    font-size: 16px;
    color: #333;
    outline: none;
    background: transparent;
}

#saisie-texte::placeholder {
    color: var(--blue-dark);
    font-size: 20px;
    font-weight: 600;
    opacity: 0.8;
}

/* --- GROUPE ACTIONS (Importer et Format) --- */
.groupe-actions-saisie {
    display: flex;
    align-items: center;
    width: 100%;
    max-width: 450px;
    margin-top: 20px;
}

/* Bouton Importer (.btn-importer) */
.btn-importer {
    background: none;
    border: none;
    color: white;
    padding: 0;
    margin-right: 30px;
    cursor: pointer;
    font-weight: 600;
    font-size: 16px;
    display: flex;
    align-items: center;
    position: relative;
    left: -20px;
}

.format-accepte {
    font-size: 14px;
    color: white;
    margin-left: 20px;
    font-weight: 600;
}

/* --- BOUTON GÉNÉRER (.btn-generer) --- */
.btn-generer {
    align-self: flex-start; 
    background-color: var(--blue-dark);
    color: var(--white);
    padding: 15px 40px;
    border: none;
    border-radius: 8px;
    font-size: 18px;
    font-weight: 600;
    cursor: pointer;
    transition: background-color 0.3s;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    margin-top: 15px;
    margin-left: -20px; 
}

.btn-generer:hover {
    background-color: #1E88E5; 
}

/* --- ICÔNE D'AIDE (.icone-aide) --- */
.icone-aide {
    position: fixed;
    bottom: 20px;
    right: 20px;
    width: 30px;
    height: 30px;
    background-color: var(--white);
    color: var(--blue-mid);
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 20px;
    font-weight: 600;
    cursor: pointer;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
}

/* Media Queries pour la réactivité */
@media (max-width: 992px) {
    main {
        flex-direction: column;
        align-items: center;
    }

    .conteneur-saisie, .conteneur-resultat {
        width: 90%;
        height: auto;
    }
    
    .box-style {
        height: 350px; 
    }
    
    .groupe-actions-saisie, .btn-generer {
        margin-left: 0;
        align-self: center; 
    }
    
    .btn-importer {
        left: 0; 
    }

    .icone-nuage {
        left: 0; 
    }
    
    .icone-nuage::before {
        display: none; 
    }
}

footer {
    /* Positionnement pour coller le footer en bas de la fenêtre */
    width: 100%; /* S'étend sur toute la largeur */
    
    /* Style visuel */
    background-color: var(--blue-dark); /* Un bleu foncé pour contraster */
    color: var(--white);
    text-align: center;
    padding: 8px 0; /* Marge interne pour la hauteur */
    font-size: 14px;
    z-index: 100; /* Assure qu'il est au-dessus des autres éléments */
    box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1);
}